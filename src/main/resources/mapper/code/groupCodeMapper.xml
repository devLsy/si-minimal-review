<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsy.sicore_v1.code.mapper.GroupCodeMapper">

    <!-- 목록 조회 -->
    <select id="selectGroupCodeList" resultType="GroupCodeVo">
        /* com.lsy.sicore_v1.code.mapper.GroupCodeMapper.selectGroupCodeList [그룹 코드 목록 조회] */
        SELECT
              GROUP_CODE
            , GROUP_NAME
            , DESCRIPTION
            , USE_YN
            , CREATED_AT
            , UPDATED_AT
            FROM TB_CODE_GROUP
    </select>

    <!-- 상세 조회 -->
    <select id="selectGroupCode" resultType="GroupCodeVo">
        /* com.lsy.sicore_v1.code.mapper.GroupCodeMapper.selectGroupCode [그룹 코드 상세 조회] */
        SELECT
              GROUP_CODE
            , GROUP_NAME
            , DESCRIPTION
            , USE_YN
            , CREATED_AT
            , UPDATED_AT
            FROM TB_CODE_GROUP
            WHERE GROUP_CODE = #{groupCode}
    </select>

    <!-- 등록 -->
    <insert id="insertGroupCode" parameterType="GroupCodeVo">
        /* com.lsy.sicore_v1.code.mapper.GroupCodeMapper.insertGroupCode [그룹 코드 등록] */
        INSERT INTO TB_CODE_GROUP
        (
              GROUP_CODE
            , GROUP_NAME
            , DESCRIPTION
            , CREATED_AT
            , UPDATED_AT
        )
        VALUES
        (
              #{groupCode}
            , #{groupName}
            , #{description}
            , SYSDATE
            , SYSDATE
        )
    </insert>

    <!-- 수정 -->
    <update id="updateGroupCode">
        /* com.lsy.sicore_v1.code.mapper.GroupCodeMapper.updateGroupCode [그룹 코드 수정] */
        UPDATE TB_CODE_GROUP
          <set>
                <if test="groupCodeVo.groupName != null and groupCodeVo.groupName != ''">
                    GROUP_NAME = #{groupCodeVo.groupName},
                </if>
                <if test="groupCodeVo.description != null and groupCodeVo.description != ''">
                    DESCRIPTION = #{groupCodeVo.description},
                </if>
          </set>
          <where>
            GROUP_CODE = #{oldGroupCode}
          </where>
    </update>

    <!-- 삭제 -->
    <update id="deleteGroupCode">
        /* com.lsy.sicore_v1.code.mapper.GroupCodeMapper.deleteGroupCode [그룹 코드 사용 중지] */
        UPDATE TB_CODE_GROUP
           SET USE_YN = 'N'
           WHERE GROUP_CODE = #{groupCode}
    </update>

</mapper>