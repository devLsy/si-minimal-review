<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsy.sicore_v1.role.mapper.RoleMapper">

    <!-- 목록 조회 -->
    <select id="selectRoleList" resultType="RoleVo">
        /* com.lsy.sicore_v1.role.mapper.RoleMapper.selectRoleList [권한 목록 조회] */
        SELECT
              ROLE_CODE
            , ROLE_NAME
            , USE_YN
            , CREATED_AT
            , UPDATED_AT
            FROM TB_ROLE
    </select>

    <!-- 상세 조회 -->
    <select id="selectRole" parameterType="String" resultType="RoleVo">
        /* com.lsy.sicore_v1.role.mapper.RoleMapper.selectRole [권한 상세 조회] */
        SELECT
              ROLE_CODE
            , ROLE_NAME
            , USE_YN
            , CREATED_AT
            , UPDATED_AT
            FROM TB_ROLE
            WHERE ROLE_CODE = #{roleCode}
    </select>

    <!-- 등록 -->
    <insert id="insertRole" parameterType="RoleVo">
        /* com.lsy.sicore_v1.role.mapper.RoleMapper.insertRole [권한 등록] */
        INSERT INTO TB_ROLE
        (
              ROLE_CODE
            , ROLE_NAME
            , CREATED_AT
            , UPDATED_AT
        )
        VALUES
        (
              #{roleCode}
            , #{roleName}
            , SYSDATE
            , SYSDATE
        )
    </insert>

    <!-- 수정 -->
    <update id="updateRole" parameterType="RoleVo">
        /* com.lsy.sicore_v1.role.mapper.RoleMapper.updateRole [권한 정보 수정] */
        UPDATE TB_ROLE
          <set>
                <if test="roleName != null and roleName != ''">
                    ROLE_NAME = #{roleName},
                </if>
          </set>
          <where>
            ROLE_CODE = #{roleCode}
          </where>
    </update>

    <!-- 삭제 -->
    <update id="deleteRole" parameterType="RoleVo">
        /* com.lsy.sicore_v1.role.mapper.RoleMapper.deleteRole [권한 사용 중지] */
        UPDATE TB_ROLE
           SET USE_YN = 'N'
           WHERE ROLE_CODE = #{roleCode}
    </update>

</mapper>